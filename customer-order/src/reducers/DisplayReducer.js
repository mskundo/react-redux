export default function DisplayReducer(state={
    name:'mashu',
    addressLine1:'lane',
    addressLine2:'lane2',
    postCode:909098,
    age:22}, action){
        const name=state.name
        const addressLine1=state.addressLine1
        const addressLine2=state.addressLine2
        const postCode=state.postCode
        const age=state.age
        
}